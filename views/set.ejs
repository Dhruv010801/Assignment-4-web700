<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= set.name %> - LEGO Set Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>

    <!-- Use navbar partial but with no page highlighted -->
    <%- include('partials/navbar', { page: "" }) %>

    <div class="container mt-4">
        <section>
            <h1><%= set.name %></h1>
            <p>Below, you will find detailed information about the set: <strong><%= set.name %></strong>.</p>

            <img src="<%= set.img_url %>" alt="<%= set.name %>" class="img-fluid mb-3" style="max-width: 375px;" />

            <ul class="list-group mb-3">
                <li class="list-group-item"><strong>Set Number:</strong> <%= set.set_num %></li>
                <li class="list-group-item"><strong>Year Released:</strong> <%= set.year %></li>
                <li class="list-group-item"><strong>Theme:</strong> <%= set.theme %></li>
                <li class="list-group-item"><strong>Number of Parts:</strong> <%= set.num_parts %></li>
            </ul>

            <a href="/lego/deleteSet/<%= set.set_num %>" class="btn btn-danger me-2">Delete Set</a>
            <a href="#" onclick="history.back(); return false;" class="btn btn-secondary">Back</a>
        </section>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
